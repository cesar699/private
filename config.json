import os

# 京东相关配置
JD_URL = "https://cart.jd.com/cart_index"
LOGIN_URL = "https://passport.jd.com/new/login.aspx"
BUY_BUTTONS = ["btn-reserve", "J-buy", "order-submit"]  # 可能的抢购按钮

# 反检测设置
USE_PROXY = False  # 是否启用代理
PROXY_LIST = ["http://127.0.0.1:7890"]  # 代理 IP 池
USE_HEADLESS = True  # 是否启用无头模式

# 时间同步
TIME_SYNC_API = "http://api.m.taobao.com/rest/api3.do?api=mtop.common.getTimestamp"

# 运行设置
RETRY_LIMIT = 5  # 失败重试次数
WAIT_TIME_RANGE = (0.1, 0.3)  # 人类行为模拟等待时间
NOTIFY_EMAIL = "your_email@example.com"  # 邮件通知地址
NOTIFY_WECHAT = "your_wechat_token"  # 微信通知 Token
